import React from 'react';
import MainMenu from '@/components/template/MainMenu';

const page = () => {
    return (
        <div>
            <MainMenu />
        </div>
    );
};

export default page;